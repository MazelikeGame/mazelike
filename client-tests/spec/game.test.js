import Monster from '../Frontend/game/Monster.js';

describe('Calculator', function() {
	it('should add numbers', () => {
    var temp = new Monster("Hei", 5, 5);
    expect(window).toBe(true);
  });
});