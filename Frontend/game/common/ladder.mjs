export default class LadderCommon {
  constructor() {
    this.x = 0;
    this.y = 0;
  }

  placeInRandomRoom(map) {
    /*
    let numRooms = map.rooms.length;
    this.initialRoom = Math.floor(Math.random() * numRooms);
    let randomDiffX = Math.floor(Math.random() * map.rooms[this.initialRoom].width); 
    this.x = map.rooms[this.initialRoom].x + randomDiffX;
    let randomDiffY = Math.floor(Math.random() * map.rooms[this.initialRoom].height); 
    this.y = map.rooms[this.initialRoom].y + randomDiffY;*/
  }
}
