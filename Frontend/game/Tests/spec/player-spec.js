/* global it, describe */
// import Player from '../../common/player';

describe('Player get function tests', () => {
  it('should get position after calling getPosition', () => {
    // let chad = new Player('chad', 100, { x: 5, y: 4 });
    // expect(chad.getPosition()).toEqual({ x: 5, y: 4 });
  });

  it('should get name of player after calling getName', () => {
  });

  it('should get the hp of player after calling getHp', () => {
  });

  it('should get the velocity of player after calling getVelocity', () => {
  });
});
