const chai = require("chai");

const game = require("../Frontend/game/input.js");

describe('Keyboard Inputs', () => {
  it('Can press D', (done) => {
    chai.assert(true, "Testing!");
    done();
  });
});
